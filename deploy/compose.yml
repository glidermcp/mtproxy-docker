services:
  mtg:
    image: ${MTG_IMAGE:-ghcr.io/glidermcp/mtproxy-docker:latest}
    container_name: mtg
    restart: unless-stopped
    ports:
      - "443:443/tcp"
    volumes:
      - ./mtg.toml:/config.toml:ro
